{
  "name": "telesales-workers",
  "version": "1.0.0",
  "type": "module",
  "description": "Telesales backend on Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "@anthropic-ai/sdk": "^0.24.0",
    "google-auth-library": "^9.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240805.0",
    "wrangler": "^3.28.0",
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  }
}
